# Drop-in for the default 80-wired.network shipped by systemd-conf.
# Apply the DHCP-provided domain name (DHCPv4 option 15) as the DNS search
# domain so it propagates to systemd-resolved and `hostname --fqdn` can
# resolve the host's fully-qualified name.
[DHCPv4]
UseDomains=yes
